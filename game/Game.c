#include "Game.h"

int main(int argc, char** argv) {
    // Fairly basic game structure
    // 1. Call "CreateGame()" to allocate mem for the game instance

    // 2. Assign the game's function pointers to the fields of "mainGame"

    // 3. Load any fonts or textures needed

    // 4. Start the main loop (call GameLoop())

    // 5. Close the window, de-init

    // 6. Unload fonts and textures, if any

    // 7. Free mainGame, anything else that was allocated

    return EXIT_SUCCESS;
}